# TODOs

## Feature Order
* Add
* Init
* Clone
* Destroy
* Sync

## Unsorted
* We should use `shellexpand` for tilde expansion bc it also can handle env vars etc
* Extract commands to modules
* add README
* CI
* What even is v1 going to have?

* check git release tags
```sh
git ls-remote --tags https://github.com/libsdl-org/SDL | tail -5
```
